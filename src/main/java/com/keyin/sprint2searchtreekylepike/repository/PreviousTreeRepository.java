package com.keyin.sprint2searchtreekylepike.repository;

import com.keyin.sprint2searchtreekylepike.model.PreviousTree;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PreviousTreeRepository extends JpaRepository<PreviousTree, Long> {
}
